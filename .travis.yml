sudo: required
services:
  - docker

script:
  - docker build -t jlopezarza/moviecomparer-frontend ./static
  - docker build -t jlopezarza/moviecomparer-api .
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push jlopezarza/moviecomparer-frontend
  - docker push jlopezarza/moviecomparer-api
